import 'package:flutter/material.dart';

class SecurityHome extends StatelessWidget {
  const SecurityHome({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('SecurityHome'));
  }
}
